// TODO: add implementation type imports

use crate::tests::types::*;

impl From<&${impl_type}> for SpecState {
    fn from(driver: &${impl_type}) -> Self {
        // TODO: implement conversion from ${impl_type} to SpecState
       todo!()
    }
}
